[build-system]
requires = [
    "cython",
    "oldest-supported-numpy",
    "setuptools>=45",
    "setuptools_scm>=6.2",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "cwinpy/_version.py"
